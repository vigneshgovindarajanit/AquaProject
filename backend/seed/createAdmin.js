require("dotenv").config();const bcrypt=require("bcryptjs");const db=require("../config/db");
(async()=>{try{await db();const hash=await bcrypt.hash("Admin@123",10);await db.query("INSERT IGNORE INTO users (name,email,password,role,isApproved,isActive) VALUES (?,?,?,?,1,1)",["Admin User","admin@pondtrack.local",hash,"admin"]);console.log("Admin account ready: admin@pondtrack.local / Admin@123");process.exit(0);}catch(e){console.error(e.message);process.exit(1);}})();
